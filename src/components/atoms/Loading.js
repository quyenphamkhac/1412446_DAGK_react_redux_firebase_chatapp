import React from 'react';

import '../_settings/_loading.css';

const loading = () => (
  <div className="lds-container">
    <div className="lds-hourglass"></div>
  </div>
);

export default loading;